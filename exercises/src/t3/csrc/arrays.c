#include <stdio.h>

int main()
{
	int array[10];
	/* variables necesarias */

	/* Recorrer e imprimir */
	for (/* ... */) {
	}

	/* Recorrer pares al revés e imprimir */
	for (/* ... */) {
	}

	/*
	 * Cambiar el elemento array[i] por el array[i+1] (con array[10] =
	 * array[0])
	 */
	for (/* ... */) {
	}

	/*
	 * Cambiar el elemento array[i] por el array[i+1] (con array[10] =
	 * array[0])
	 */
	for (/* ... */) {
	}
}
